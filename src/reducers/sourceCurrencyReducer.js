import { physicalCurrencies } from '../currencies';

const initialState = {
  currency: '',
  amount: 0
};

const sourceCurrencyReducer = (state = initialState, action) => {
  // switch(action.type) {
  //   default:
  //     return state;
  // }
  return state;
}

export default sourceCurrencyReducer;

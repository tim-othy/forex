const initialState = {
  currency: '',
  amount: 0
};

const targetCurrencyReducer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default targetCurrencyReducer;

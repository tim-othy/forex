const initialState = {
  exchangeRate: 0
}

const exchangeRateReducer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default exchangeRateReducer;
